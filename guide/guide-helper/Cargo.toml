[package]
name = "guide-helper"
version = "0.1.0"
publish = false
edition = "2024"
license = "MIT OR Apache-2.0"
readme = "README.md"

[dependencies]
cargo_toml = "0.22"
mdbook-preprocessor = { version = "0.5.2" }
semver = "1.0.27"
serde_json = "1.0.149"
